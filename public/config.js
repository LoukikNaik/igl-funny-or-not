// Backend API base for the DEPLOYED frontend.
//
// Local dev: leave '' — `node server.js` serves the API on the same origin.
// Production: DO NOT edit this file. The GitHub Actions deploy overwrites it
// with the value of the repo variable API_BASE
//   (repo Settings -> Secrets and variables -> Actions -> Variables),
//   set it to your public backend URL, e.g. your ngrok https URL.
//   Change the URL there and re-run the "Deploy frontend" workflow — no code commit.
window.API_BASE = '';
